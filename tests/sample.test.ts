import { assert } from 'chai';
describe('Sample Suite', async () => {
  beforeEach(async () => {});
  afterEach(async () => {});
  it('Sample', async () => {
    let sample = true;
    assert.isTrue(sample, 'Sample test ok.');
  });
});
