import { assert, expect } from 'chai';
describe('Sample Suite', async () => {
    beforeEach(async () => {});
    afterEach(async () => {});
    it('Sample', async () => {
        assert.isTrue(true, 'Sample test ok.');
    });
});
